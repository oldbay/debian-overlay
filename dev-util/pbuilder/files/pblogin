#/bin/bash

. /etc/pb.cfg

if [ ! -f $tgz_name ];then
    echo "Please rum \"pdinit\" from current directory"
    exit 1
fi

sudo env PBROOT=$PBROOT /usr/sbin/pbuilder login --basetgz $tgz_name --bindmounts $PBROOT --save-after-login
